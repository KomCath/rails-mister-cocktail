<h1>le wagon Cocktail Bar</h1>
<ol>
  <% @cocktails.each do |cocktail| %>
    <li><%= link_to cocktail.name, cocktail_path(cocktail) %> 💣<%= link_to 'Delete this cocktail', cocktail_path(cocktail), method: :delete, data: { confirm: "Are you super duper sure?" }%></li>
  <%end %>
</ol>

<p>🍸<%= link_to 'Name a new cocktail!', new_cocktail_path %></p>
